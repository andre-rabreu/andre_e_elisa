import 'package:flutter/material.dart';

class TimelineEntryWidget extends StatelessWidget {
  const TimelineEntryWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}